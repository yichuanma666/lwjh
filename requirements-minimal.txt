# 最低兼容版本的依赖
# 如果服务器环境较旧，pip 无法找到新版本，使用此文件
# 使用方法: pip install -r requirements-minimal.txt
#
# 这个文件使用最低的版本要求，确保在旧系统上也能安装

requests>=2.25.0
PyYAML>=5.4.0
dataclasses-json>=0.5.7
tenacity>=8.0.0
fastapi>=0.68.0
uvicorn[standard]>=0.15.0
jinja2>=3.0.0
python-multipart>=0.0.5




