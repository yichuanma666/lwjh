# 修复日志路径错误

## 🔍 错误说明

错误信息：`nginx: [emerg] open() "/var/log/nginx/qk-paper-search-access.log" failed (2: No such file or directory)`

**原因**：配置文件使用了标准 Nginx 日志路径 `/var/log/nginx/`，但宝塔面板使用的是 `/www/server/nginx/logs/`

## ✅ 快速修复

### 方法 1：使用修复后的脚本

```bash
# 重新运行修复脚本（已更新，会自动修正日志路径）
sudo bash fix-bt-nginx-site.sh
```

### 方法 2：手动修复日志路径

```bash
# 编辑配置文件
sudo vi /www/server/panel/vhost/nginx/qk-paper-search.conf
```

将日志路径从：
```nginx
access_log /var/log/nginx/qk-paper-search-access.log;
error_log /var/log/nginx/qk-paper-search-error.log;
```

改为：
```nginx
access_log /www/server/nginx/logs/qk-paper-search-access.log;
error_log /www/server/nginx/logs/qk-paper-search-error.log;
```

### 方法 3：使用 sed 快速替换

```bash
# 替换日志路径
sudo sed -i 's|/var/log/nginx/|/www/server/nginx/logs/|g' /www/server/panel/vhost/nginx/qk-paper-search.conf

# 验证修改
cat /www/server/panel/vhost/nginx/qk-paper-search.conf | grep log

# 测试配置
/www/server/nginx/sbin/nginx -t

# 重新加载
/www/server/nginx/sbin/nginx -s reload
```

## 📝 正确的宝塔配置

```nginx
server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    location /static {
        alias /opt/qk/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 使用宝塔日志路径
    access_log /www/server/nginx/logs/qk-paper-search-access.log;
    error_log /www/server/nginx/logs/qk-paper-search-error.log;
}
```

## 🚀 一键修复命令

```bash
# 一键修复日志路径并重新加载
sudo sed -i 's|/var/log/nginx/|/www/server/nginx/logs/|g' /www/server/panel/vhost/nginx/qk-paper-search.conf && \
/www/server/nginx/sbin/nginx -t && \
/www/server/nginx/sbin/nginx -s reload && \
echo "✅ 日志路径已修复并重新加载！"
```

## 📚 相关文档

- [fix-bt-nginx-site.sh](fix-bt-nginx-site.sh) - 已更新，自动修正日志路径
- [部署到宝塔面板-完整步骤.md](部署到宝塔面板-完整步骤.md) - 完整部署步骤
- [宝塔面板Nginx配置说明.md](宝塔面板Nginx配置说明.md) - 宝塔配置说明

---

**快速修复**：
```bash
sudo sed -i 's|/var/log/nginx/|/www/server/nginx/logs/|g' /www/server/panel/vhost/nginx/qk-paper-search.conf && /www/server/nginx/sbin/nginx -t && /www/server/nginx/sbin/nginx -s reload
```




